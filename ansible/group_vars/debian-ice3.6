# INTERNAL CONFIG DON'T CHANGE

path: "{{ playbook_dir }}"
clean:
os: debian
ice_version: "3.6"

doc: False
doc_conf:
  header: "OMERO.web deployed separately from OMERO.server Debian 9 and IcePy 3.6\n======================================================================\n"
  link_install: "Please first read :doc:`../../server-debian9-ice36`.\n"
  os_full_name: Debian

default:
  web_prefix:
  web_port: 80
  web_server_name: localhost

omero_user: "omero"
omero_user_home_dir: "/home/{{ omero_user }}"
virtualenv_path: "{{ omero_user_home_dir }}/omerowebvenv"

selinux: False
daemon: True

# VARS

omero_version: "OMERO-DEV-latest"
system_site_packages: True
web_session:

web_prefix:
web_port:
web_server_name:
