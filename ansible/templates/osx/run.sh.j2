{% if doc %}
Start up services::
{% else %}
#!/bin/bash

set -e -u -x
{% endif %}

{% if doc %}
source {{ virtualenv_path }}/bin/activate
{% else %}
set +u
source {{ virtualenv_path }}/bin/activate
set -u
{% endif %}

# Start OMERO.web
{{ omero_user_home_dir }}/OMERO.py/bin/omero web start
