{% if doc %}
Install dependencies and add scl Python 3.8 to your PATH::
{% else %}
#!/bin/bash

set -e -u -x
{% endif %}

yum -y install epel-release

yum -y install unzip

yum -y install nginx

yum -y install centos-release-scl
yum -y install rh-python38-python
