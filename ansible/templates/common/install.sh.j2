{% if doc %}
Upgrade pip and install OMERO.web::

{% else %}
#!/bin/bash

set -e -u -x

{% endif %}

{{ virtualenv_path }}/bin/pip install --upgrade pip
{{ virtualenv_path }}/bin/pip install git+https://github.com/jburel/omero-web.git@remove_pkg
