{% if doc %}
Install OMERO.web::
{% else %}
#!/bin/bash

set -e -u -x

OMEROVER=${OMEROVER:-{{ omero_version }}}
export OMERODIR=$OMERODIR
export PATH={{ virtualenv_path }}/bin:$PATH
mkdir -p $OMERODIR/etc/grid


{% endif %}

pip install "git+git://github.com/snoopycrimecop/omero-web@merge_ci#egg=omero-web"
