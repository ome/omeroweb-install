{% if doc %}
Upgrade pip and install OMERO.web:

.. parsed-literal::

    /opt/omero/web//venv3/bin/pip install --upgrade pip
    /opt/omero/web//venv3/bin/pip install "omero-web>=\ |version_web|"

{% else %}
#!/bin/bash

set -e -u -x

{{ virtualenv_path }}/bin/pip install --upgrade pip
{{ virtualenv_path }}/bin/pip install "omero-web>=5.6.1"

{% endif %}
