{% if doc %}
Install OMERO.web::
{% else %}
#!/bin/bash

set -e -u -x

OMEROVER=${OMEROVER:-{{ omero_version }}}
export OMERODIR=$OMERODIR
mkdir -p $OMERODIR/etc/grid


{% endif %}

{{ virtualenv_path }}/bin/pip install "omero-web>=5.6.dev5"
