{% if doc %}
For convenience the main OMERO.web configuration options have been defined as environment variables. You can either use your own values, or alternatively use the following ones::

{% if not doc %}
OMERO_USER={{ omero_user }}
OMERODIR={{ omero_user_web_dir }}

{% endif %}
{% if web_session %}
export WEBSESSION={{ web_session }}
{% endif %}
{% if default.web_prefix %}
export WEBPREFIX={{ default.web_prefix }}
{% endif %}
# If you are installing OMERO.web and OMERO.server on the same machine.
# Point OMERODIR to the OMERO.server
# export OMERODIR=/path_to_omero_server/OMERO.server
export OMERODIR={{ omero_user_web_dir }}
export WEBPORT={{ default.web_port }}
export WEBSERVER_NAME={{ default.web_server_name }}
{% else %}
OMERO_USER=${OMERO_USER:-{{ omero_user }}}
OMERODIR={{ omero_user_web_dir }}

WEBSESSION=${WEBSESSION:-{{ web_session }}}

WEBPREFIX=${WEBPREFIX:-{{ default.web_prefix }}}
WEBPORT=${WEBPORT:-{{ default.web_port }}}
WEBSERVER_NAME=${WEBSERVER_NAME:-{{ default.web_server_name }}}
{% endif %}
