{% if doc %}
Create the virtual. This is the recommended way to install OMERO.web::

python3 -mvenv {{ virtualenv_path }}

{% else %}
#!/bin/bash

set -x

python3 -mvenv {{ virtualenv_path }}

{% endif %}
